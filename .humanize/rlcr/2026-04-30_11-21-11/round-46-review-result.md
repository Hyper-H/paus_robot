- [P2] Archive the saved manual trajectory into the session directory — /home/chen_lab/worktrees/paus_robot_handeye_rlcr/src/paus_marker_ros2/paus_marker_ros2/eye_to_hand_calibration_node.py:942-948
  The semi-auto path copies `trajectory_used.yaml` into each session, but the manual `save_trajectory` path only writes `self.trajectory_path` and publishes status. If the operator later edits or overwrites that YAML, the archived manual session no longer contains the exact trajectory that was saved, so later inspection has to reconstruct it from `run.log` heuristics instead of reading the original file. Copying the saved trajectory into the current session here would make manual archives self-contained like semi-auto ones.

- [P3] Map the semi_auto_started status into a workflow stage — /home/chen_lab/worktrees/paus_robot_handeye_rlcr/src/paus_ui/paus_ui/ros_bridge.py:640-649
  The calibration node publishes `semi_auto_started` before the first waypoint update, but `_workflow_for_status()` does not recognize it and falls back to the raw status string. In the UI, `renderFlow()` has no step for that value, so the workflow bar is blank from the moment the operator presses Run until the first `waypoint_motion_started`/`waypoint_dry_run_complete` event arrives. Adding a mapping here would keep the progress indicator accurate during startup.
2026-05-01T10:45:02.144604Z ERROR codex_core::session: failed to record rollout items: thread 019de314-783c-7e33-ae59-629e3908f28c not found
2026-05-01T10:45:02.154880Z ERROR codex_core::session: failed to record rollout items: thread 019de314-781b-7d80-ae74-f64d3c641220 not found
The new hand-eye UI/session work is largely coherent, but there are still a couple of functional gaps: manual trajectory sessions are not archived with their saved YAML, and the UI workflow state machine drops the initial semi-auto start state. Those issues make the archive incomplete and the operator feedback inaccurate.

Full review comments:

- [P2] Archive the saved manual trajectory into the session directory — /home/chen_lab/worktrees/paus_robot_handeye_rlcr/src/paus_marker_ros2/paus_marker_ros2/eye_to_hand_calibration_node.py:942-948
  The semi-auto path copies `trajectory_used.yaml` into each session, but the manual `save_trajectory` path only writes `self.trajectory_path` and publishes status. If the operator later edits or overwrites that YAML, the archived manual session no longer contains the exact trajectory that was saved, so later inspection has to reconstruct it from `run.log` heuristics instead of reading the original file. Copying the saved trajectory into the current session here would make manual archives self-contained like semi-auto ones.

- [P3] Map the semi_auto_started status into a workflow stage — /home/chen_lab/worktrees/paus_robot_handeye_rlcr/src/paus_ui/paus_ui/ros_bridge.py:640-649
  The calibration node publishes `semi_auto_started` before the first waypoint update, but `_workflow_for_status()` does not recognize it and falls back to the raw status string. In the UI, `renderFlow()` has no step for that value, so the workflow bar is blank from the moment the operator presses Run until the first `waypoint_motion_started`/`waypoint_dry_run_complete` event arrives. Adding a mapping here would keep the progress indicator accurate during startup.
